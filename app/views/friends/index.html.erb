<div class="grid">
  <%@friends.each do |friend|%>
    <%= render partial: "card", locals:{friend: friend } %>
  <%end%>
</div>
<template id="new_friend-form">
  <%=render partial: 'friends/form', locals:{obj: Friend.new}%>
</template>